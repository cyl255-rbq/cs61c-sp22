标签 - 用于区分使用相同索引的不同块。位数： (内存地址中的位数) - 索引位数 - 偏移位数
索引 - 这块内存将被放置的集合。位数： log2(索引数量)
偏移 - 块中字节的 location。位数： log2(块大小)
根据这些定义，以下情况成立：
log2(内存大小) = 地址位宽 = 标签位数 + 索引位数 + 偏移位数
另一个值得记住的有用等式是：
缓存大小 = 块大小 ∗ 块数量

容量为 32B、块大小为 8B 的直接映射字节寻址缓存。在每个地址的 32 位中
偏移位占了 3 位，索引位占了 2 位（共 5 位），那么标签位就是剩下的第 5 到 31 位。