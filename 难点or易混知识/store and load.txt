Load 指令（加载）：数据是从“窄”的地方（内存中的 8 位或 16 位）流向“宽”的地方（寄存器的 32 位）。
为了填满那 32 位并保持数值的数学意义，必须进行符号位扩展（Sign Extension）。
Store 指令（存储）：数据是从“宽”的地方（寄存器）流向“窄”的地方（内存）。这本质上是一个截断（Truncation）过程。
CPU 只需要取出寄存器中最低的 8 位或 16 位，直接“压”进内存地址即可。