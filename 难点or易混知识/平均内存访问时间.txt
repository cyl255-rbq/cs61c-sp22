AMAT = 命中时间 + 未命中率 * 未命中罚
处理器总是首先检查缓存。如果命中，则只需访问缓存（因此公式中包含了命中时间）。如果未命中，
则缓存必须先访问下一级内存（如果只有一个缓存，则访问主内存；否则访问下一级缓存 L2、L3 等）
来获取数据块（未命中惩罚是指未命中时访问下一级内存的惩罚）。

类似的二级缓存公式:
AMAT=L1 命中时间 + (L1未命中率)*(L2 命中时间+(L2 未命中率)*(未命中惩罚))