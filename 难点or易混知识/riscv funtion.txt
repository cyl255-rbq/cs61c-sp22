Write a function sumSquare in RISC-V that, when given an integer n, returns the
summation below. If n is not positive, then the function returns 0.
n^2 + (n − 1)^2 + (n − 2)^2 + . . . + 1^2

针对这个问题，你将得到一个名为square的RISC-V函数，该函数接收一个
单个整数并返回其平方。首先，我们来实现函数的核心部分：平方和求和。我们将
遵循调用者/被调用者约定，因此我们预计参数会存储在哪个寄存器中参数n？
哪些寄存器应保存平方的参数和返回值？我们应将sumSquare的返回值存入哪个寄存器？

	add s0, a0, x0 	# Set s0 equal to the parameter n
	add s1, x0, x0 	# Set s1 (accumulator) equal to 0
loop: beq s0, x0, end # Branch if s0 reaches 0
	add a0, s0, x0 	# Set a0 to the value in s0, setting up
				# args for call to function square
	jal ra, square 	# Call the function square
	add s1, s1, a0 	# Add the returned value into s1
	addi s0, s0, -1 	# Decrement s0 by 1
	jal x0, loop 	# Jump back to the loop label
end: add a0, s1, x0 	# Set a0 to s1 (desired return value)

由于sumSquare是被调用者，我们需要确保它不会覆盖任何寄存器 
呼叫者可以使用。考虑到你上面的实现，写一个序言和 
结语，说明你使用的寄存器。

prologue: addi sp, sp -12 	# Make space for 3 words on the stack
		sw ra, 0(sp) 	# Store the return address
		sw s0, 4(sp) 	# Store register s0
		sw s1, 8(sp) 	# Store register s1
epilogue: lw ra, 0(sp) 	# Restore ra
		lw s0, 4(sp) 	# Restore s0
		lw s1, 8(sp) 	# Restore s1
		addi sp, sp, 12 	# Free space on the stack for the 3 words
		jr ra 			# Return to the caller

请注意，ra存储在序言和结语中，即使它是一个调用者保存的寄存器。
这是因为，如果我们在sumSquare的主体中调用多个函数，
我们需要在每次调用时将ra保存到堆栈中，这是多余的——我们不妨将其保存在序言中，
并在结语中与被调用者保存的寄存器一起恢复。因此，在不调用其他函数的函数中，
只要没有其他内容覆盖ra，通常可以安全地避免在序言/结语中保存/恢复ra。

因为他是函数里调用函数，所以要存ra，然后就是因为函数会用到t0之类的，所以要用s0,s1保存n和sum
然后呢，因为你需要用到s0,s1就得把之前的这两个的值保存，就得用sp来减地址来存，sp相当于内存的最高点
然后a0-a7为参数，函数return的值保存在a0,a1里，先是a0，a0不够才是a1，所以每次调用函数前先
把参数存进a0-a7里，然后算出的结果存进a0里，调用函数的时候使用jal ra xxx，否则丢失回来的路
等到一切完事后然后别忘了恢复之前存进sp里的东西